name: daily-bulletin-worker
main: workers/worker-api.js
compatibility_date = "2026-01-01"

# Bindings — when deploying in Cloudflare dashboard, bind the D1 database to the variable name "DB"
# and set an environment variable ADMIN_TOKEN to protect admin operations.
